fn add(x, y) {
    return {x + y};
}

fn subtract(x, y) {
    return {x - y};
}

fn multiply(x, y) {
    return {x * y};
}

fn square(x) {
    return {x * x};
}

fn divide(x, y) {
    return {x / y};
}
