fn add(x, y) {
    return {x + y};
}

fn subtract(x, y) {
    return {x - y};
}

fn multiply(x, y) {
    return {x * y};
}

fn pow(x, base) {
    y = x;
    for i = 2; i <= base; ++i; {
	y = y * x;
    }
    return {y};
}

fn divide(x, y) {
    return {x / y};
}
